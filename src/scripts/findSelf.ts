import MiniMap from "../screen/MiniMap";

(() => {
    const miniMap = new MiniMap({
        height: 1,
        width: 60,
        oHeight: 130,
        oWidth: 0
    })
    miniMap.getCharacter()
})()
